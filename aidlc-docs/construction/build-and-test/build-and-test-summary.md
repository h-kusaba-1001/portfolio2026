# Build and Test Summary

**実施日**: 2026-08-22 / 2026-08-23
**対象**: UoW-1〜4（全ユニット）
**公開 URL**: https://d3bttkxchvfb66.cloudfront.net

---

## ビルド

| 項目 | 結果 |
|---|---|
| ビルドツール | Vite 8.2.2 / Composer 2.10.2 |
| **ビルド状態** | **成功** |
| 生成物 | `public/build/`（JS 331.72 kB / gzip 105.42 kB、CSS 42.05 kB / gzip 8.31 kB） |
| ビルド時間 | 0.6 秒 |
| 型チェック | `tsc --noEmit` エラーなし |

---

## テスト実行結果

### ユニット + Feature テスト

| 項目 | 結果 |
|---|---|
| **総数** | **35** |
| **成功** | **35** |
| 失敗 | 0 |
| アサーション | 177 |
| 実行時間 | 0.69 秒 |
| カバレッジ | **測定していない**（理由は unit-test-instructions.md） |
| **状態** | **Pass** |

### 結合テスト

**独立したスイートは作っていない**（プロセスをまたぐ結合点が存在しないため）。
ユニット間の 6 つの結合点は Feature テストが実物を通して検証している。

| 項目 | 結果 |
|---|---|
| 検証している結合点 | 6 |
| **状態** | **Pass** |

デプロイ後の手動確認（I-1〜I-5）も 2026-08-22 に実施済み。全て期待どおり。

### 性能テスト

**実施しない。** 性能目標が存在せず（U1-PF-5 / U1-SC-3）、
負荷をかけること自体が NFR-1（費用）と衝突するため。

| 実測値 | 値 |
|---|---|
| Lambda 実行時間 | 18.8 ms |
| Lambda 使用メモリ | 138 MB / 512 MB |

| 項目 | 結果 |
|---|---|
| **状態** | **N/A（目標なし）** |

### セキュリティテスト

| 項目 | 結果 |
|---|---|
| 依存の脆弱性（composer / npm） | **0 件** |
| 自動テストで担保 | SECURITY-04 / 05 相当 / 09 / 15 |
| デプロイ後の手動確認 | ヘッダ 5 件 / HTTPS リダイレクト / S3 非公開 |
| **未実施** | **SBOM（CycloneDX）— backlog S-1** |
| **状態** | **Pass（ただし SBOM が未達）** |

### 契約テスト / E2E テスト

| 項目 | 結果 |
|---|---|
| 契約テスト | **N/A**（公開 API を持たない） |
| E2E テスト | **N/A**（ブラウザテストを書かない方針: ADR-009） |

---

## 全体の状態

| 項目 | 結果 |
|---|---|
| ビルド | **成功** |
| 全テスト | **Pass（30/30）** |
| **Operations に進める状態か** | **条件付きで Yes** |

### 「条件付き」の中身

**既に本番稼働している**（UoW-1〜4 とも公開済み）。
そのうえで、以下が残っている。

| # | 内容 | 影響 |
|---|---|---|
| **B-4** | **目視確認が未実施。** 構成図の見え方・粒の速度・375px の縦積み・ダークモードを人の目で見ていない | 機械では担保できない領域。**最優先** |
| **S-1** | SBOM 未実施 | SECURITY-10 の一部が未達 |
| **P-2** | HTML が CloudFront にキャッシュされない | U1-PF-4 と NFR-S9 のキャッシュ部分が未達。費用の天井は同時実行上限が担保 |
| **V-7** | 本番ログの JSON 形式が未確認 | 機能影響なし。検索性の問題 |
| **C-1** | AI-DLC を取り入れたことがサイトに書かれていない | 訴求上の未完 |

**全て `docs/backlog.md` で管理している。**

---

## 完了条件（`docs/requirements.md` §7）との対応

| 完了条件 | 状態 |
|---|---|
| CloudFront のデフォルトドメインで公開されている | ✅ |
| スマートフォンで全セクションが読める | ⚠️ **構造的には満たすが目視未確認**（B-4） |
| 構成図のアニメーションが動き、各ノードから選定理由が開く | ⚠️ 実装済み。**目視未確認**（B-4） |
| `prefers-reduced-motion` に対応している | ✅ CSS とフックの両方で担保 |
| 構成図がページ最上部にある（D-4） | ✅ |
| 各セクションが「結論を一言」→「補足」の 2 段（D-1） | ✅ |
| セクションごとにレイアウトが変えてある（D-2） | ✅ |

**残る 2 つはいずれも「目視確認」。** ADR-009 でブラウザテストを書かない方針にした結果、
ここは人の目でしか埋められない。

---

## 次の一手

1. **公開サイトを実際に見る**（B-4）— 完了条件のうち機械で埋められない 2 つがここで決まる
2. SBOM の追加（S-1）— やると決めたのにやっていない唯一の項目
3. P-2 / V-7 / I-1 / I-2 / C-1 — `docs/backlog.md` の優先度に従う
