# 要件定義

## 1. ゴール

サイトを見た人が、**技術構成について直接質問したくなる**状態をつくる。

「見た目がきれいですね」で終わらせない。
「なぜデータベースを使わなかったのか」「なぜ osls を選んだのか」を
聞きたくなることをもって成功とする。

そのために、技術力の証明を「作れる」ではなく **「選べる」** に置く。
何が作れるかではなく、制約の中でどう判断したかを見せる。

## 2. 非ゴール

- 機能の多さ（認証・CRUD・管理画面などは差がつかない）
- デザインのオリジナリティ勝負（見やすさが担保できれば十分）
- 実務案件の詳細（職務経歴書の役割。重複させない）

## 3. 非機能要件

| ID | 要件 |
|---|---|
| NFR-1 | 月額固定費をゼロに近づける（目標: 月 100 円以下） |
| NFR-2 | RDS・NAT Gateway・ALB を使用しない |
| NFR-3 | ライセンス費用・アカウント登録が不要なツールで構成する |
| NFR-4 | スマートフォンでの閲覧に耐えること |
| NFR-5 | 独自ドメインは取得しない。CloudFront のデフォルトドメインで公開する |
| NFR-6 | サーバサイドは要件が生じるまで実装しない。生じた場合に Lambda 関数の追加で拡張できる状態を保つ |

### セキュリティ非機能要件

AI-DLC の Security ベースライン拡張を有効にしたことによる要件（ADR-010）。
括弧内は対応する SECURITY ルール ID。

| ID | 要件 |
|---|---|
| NFR-S1 | HTML を返す全レスポンスに CSP・HSTS・X-Content-Type-Options・X-Frame-Options・Referrer-Policy を付与する（SECURITY-04） |
| NFR-S2 | CloudFront と API Gateway のアクセスログを有効にする（SECURITY-02） |
| NFR-S3 | アプリケーションログを構造化し、CloudWatch Logs に集約する。ログ保持期間は 14 日とする（SECURITY-03 / SECURITY-14 のログ保持要件は適用外: ADR-014） |
| NFR-S4 | IAM ポリシーは最小権限とし、ワイルドカードのアクション・リソースを使わない（SECURITY-06） |
| NFR-S5 | 依存はロックファイルで固定し、脆弱性スキャンをビルド手順に含める。Docker イメージタグを `latest` で固定しない（SECURITY-10） |
| NFR-S6 | 本番でスタックトレース・内部パス・フレームワークバージョンを返さない。グローバルエラーハンドラを持つ（SECURITY-09, SECURITY-15） |
| NFR-S7 | S3 バケットはパブリックアクセスをブロックし、CloudFront 経由でのみ配信する（SECURITY-01, SECURITY-09） |
| ~~NFR-S8~~ | ~~CloudFront を経由しないリクエストを拒否する~~ → **取り消し**（ADR-015）。セキュリティヘッダは Laravel のミドルウェアで付与するため、オリジン遮断は不要になった。オリジン迂回によるキャッシュバイパスは既知の未対応事項として構成図の拡張ポイントに記載する |
| NFR-S9 | 濫用対策として、CloudFront のキャッシュ・Lambda 同時実行数の上限・予算アラートを設ける。AWS WAF は使わない（SECURITY-11 / ADR-013） |

**NFR-1 との関係**: NFR-S2・NFR-S3 はログ保存に伴う費用が発生する。この規模では
月額数円〜十数円の範囲に収まる見込みで、NFR-1 の「月 100 円以下」とは両立する。
両立しないことが判明した場合は ADR-010 を見直す。

## 4. 技術構成

| レイヤ | 採用技術 | ADR |
|---|---|---|
| デプロイ | osls | ADR-001 |
| ランタイム | Bref 3.0 + AWS Lambda（PHP 8.4） | ADR-007 |
| アプリ | Laravel + Inertia.js | ADR-004 |
| フロント | React + TypeScript + Tailwind CSS（CSR。SSR なし） | ADR-006, ADR-008 |
| 配信 | Lift `server-side-website` | ADR-005 |
| データ | Markdown（`league/commonmark`） | ADR-002, ADR-003 |
| 開発環境 | Laravel Sail（Docker、PHP 8.4） | ADR-007 |
| テスト | Pest（ユニット + Feature） | ADR-009 |

## 5. サイト構成

### S-1. Hero + 構成図（一体）

- サイト名: **HK Portfolio**
- キャッチ: **月額 100 円未満で動く、サーバレスなポートフォリオ**
- リポジトリへのリンク: https://github.com/h-kusaba-1001/portfolio2026
- **アーキテクチャ構成図をこの位置に置く**（D-4。2026-08-22 変更）

**変更前**: Hero のあとに S-2 として構成図を置いていた。
**変更後**: 開いて最初に構成図が目に入るよう、Hero と一体で最上部に置く。

Hero の文言とリンクは React コンポーネントに直接持つ（Markdown 化しない）。
分量が少なく、更新頻度も低いため。US-7 の「コードを触らずに更新」の対象は S-2〜S-5 とする。

Lambda の月 100 万リクエストと CloudFront のデータ転送には永年無料枠があるため、
構成上は 0 円近辺に収まる見込み。AWS の請求額と食い違ったらキャッチを差し替える。

**根拠より小さい数字は書かないこと。**

### S-2. 技術構成 ★最大の見せ場

**構成図そのものは S-1 に移した**（D-4）。ここが担うのは図の「読み解き」。

- 構成図の各ノードを選んだ理由（`content/stack.md` から供給。原典は ADR）
- 拡張ポイントの説明

技術力が最大の訴求軸のため、経歴より前に配置する。

**変更（2026-08-23）**: 独立した「選ばなかったもの」セクション（RDS / NAT Gateway /
ALB / WAF / SSR / X-Ray の一覧）は**削除した**。
§1 のゴール「技術力の証明を『作れる』ではなく『選べる』に置く」は、
各ノードの選定理由（`content/stack.md`）が引き続き担う。

### S-3. やってきたこと（抜粋）

箇条書き。所属先の固有名詞は伏せ、業種・規模・期間・技術名は残す。

### S-4. キャリアの変遷

「何を得たか」で語る。

### S-5. これから

今後どういう仕事に軸足を置きたいか。

### S-6. Contact（廃止）

独立した Contact セクションは設けない。連絡先は Hero に置く
GitHub リポジトリへのリンクに集約する。

## 5.5. デザイン要件（2026-08-22 追加）

初回デプロイ後のレビューで「**テキストが多すぎて、ぱっと見で分かりにくい**」
「レイアウトが単調」という指摘を受けたため、見せ方の要件を追加する。

### D-1. 結論を先に、大きく

**各セクションは「結論を一言」→「補足」の 2 段構成にする。**

| 段 | 内容 | 見た目 |
|---|---|---|
| 結論 | **何が言いたいのかを一言で** | 大きく、太く |
| 補足 | その根拠・詳細 | 現在と同じ本文サイズ |

読み手が**上の一言だけ拾って流し読みできる**こと。
本文まで読むかどうかは読み手が選べる状態にする。

### D-2. 単調さを避ける

全セクションを同じ「見出し + 本文」の縦積みにしない。
**セクションごとにレイアウトを変える**（左右分割・カード・年表・図）。

### D-3. 図を使う

文章で説明している内容のうち、図にできるものは図にする。
**外部画像は読み込まない**（CSP `img-src 'self' data:` / SECURITY-13）。
SVG をコンポーネントとして持つか、`public/` に自前で置く。

### D-4. 構成図をページ最上部に置く

**構成図は「あとで出てくる図」ではなく、サイトを開いて最初に目に入るもの**にする。
このサイトの主張は技術構成そのものであり、それを最初に見せるのが最も分かりやすい。

### 非ゴールとの関係

§2 の非ゴール「デザインのオリジナリティ勝負はしない」は維持する。
**求めているのは独自性ではなく可読性。**
スライドのように「一目で要点が分かる」ことを目指す。

---

## 6. 目玉：リクエストの流れのアニメーション

要件の中核。`ArchitectureDiagram` コンポーネントに実装する。

**やること**

1. 構成図を SVG で描く
   `Browser → CloudFront → API Gateway → Lambda (Bref) → Laravel → Response`
   S3（静的アセット）は CloudFront から分岐する枝として描く
2. 光の粒がノード間を順に流れるアニメーション。ループさせる
3. 各ノードをクリック / タップすると選定理由がパネルで開く
   内容は `content/stack.md` から供給する（ハードコードしない）
4. 「拡張ポイント」を破線で表示
   - Lambda から DynamoDB / SQS / 外部 API
   - Bref X-Ray（将来）
   - 「要件が無い層は作っていない。必要になれば関数を足すだけで拡張できる」という
     設計判断の説明を添える

**やらないこと**

- 動きを他セクションに広げない。目玉は一つに絞る
- 他はスクロールフェードインに留める

`prefers-reduced-motion` を尊重し、アニメーションを止めても情報が読める状態を保つ。

## 7. 完了条件

最終確認日: **2026-08-23**（backlog B-3）

- [x] CloudFront のデフォルトドメインで公開されている
      → `https://d3bttkxchvfb66.cloudfront.net/` が 200 を返す
- [ ] スマートフォンで全セクションが読める
      → 縦積みレイアウト（`NARROW`）とレスポンシブ指定は実装済み。
      **実機での可読性は目視確認が必要**（B-4）
- [ ] 構成図のアニメーションが動き、各ノードから選定理由が開く
      → `FlowParticle`（`<animateMotion>`）とノード選択（`NodePanel`）を実装済み。
      パネル本文の見出しが `content/stack.md` に実在することは `DiagramNodesTest` が検証している。
      **動きの見え方は目視確認が必要**（B-4）
- [x] `prefers-reduced-motion` に対応している
      → `usePrefersReducedMotion` フック、`FlowParticle` の分岐、`app.css` の 3 箇所で対応
- [x] **構成図がページ最上部にある**（D-4）
      → Hero の直後、最初のセクション（`Stack`）の冒頭に配置
- [x] **各セクションが「結論を一言」→「補足」の 2 段になっている**（D-1）
      → 4 セクション全てが `SectionLead` を使用
- [x] **セクションごとにレイアウトが変えてある**（D-2）

| セクション | レイアウト |
|---|---|
| 技術構成 | 枠付きカードの中に横長の構成図 |
| やってきたこと | 枠付きカード |
| キャリアの変遷 | 縦のタイムライン（丸の遠近感＋上向きの矢印） |
| これから | 5 列グリッド |

**残る 2 項目はいずれも「実装は完了しており、人の目で見て確認する」段階。**
ブラウザテストを書かない方針（ADR-009）のため機械では担保できない。
